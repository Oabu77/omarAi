{
  "name": "@omar-ai/secret-manager-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Read-minimized MCP connector for secret metadata and hygiene auditing.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check api/mcp.js && node --check api/healthz.js && node --check src/config.js && node --check src/http.js && node --check src/mcp.js && node --check src/security.js && node --check src/vercel.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.76"
  }
}
